# `_.forEachRight()`

`_.forEachRight(collection, [iteratee=_.identity])`

This method is like `_.forEach except that it iterates over elements of collection from right to left.

## Arguments
`collection (Array|Object)`: The collection to iterate over.  
`[iteratee=_.identity] (Function)`: The function invoked per iteration.

## Returns
`(*)`: Returns collection.

## Example

import Embed from 'runkit-embed-react';

export const Demo = () => {
var code =  `var forEachRight = require("lodash/forEachRight");
forEachRight([1, 2], function(value) {
  console.log(value);
});
// => Logs '2' then '1'.
`   
  return (
    <div>
      <Embed source={code} />
    </div>
  );
};

<Demo/>

## Deep

export const DeepDemo = () => {
var code =  `function forEachRight(collection, iteratee) {
  const func = Array.isArray(collection) ? arrayEachRight : baseEachRight
  return func(collection, iteratee)
}
forEachRight([1, 2], function(value, key) {
  console.log(key);
});
// => Logs '2' then '1'.
`   
  return (
    <div>
      <Embed source={code} />
    </div>
  );
};

<DeepDemo/>



