# `_.isArray(value)`

Checks if value is classified as an Array object.

## Arguments

`value (*)`: The value to check.

## Returns

`(boolean)`: Returns `true` if `value` is an array, else `false`.

## Example

import Embed from "runkit-embed-react";

export const Demo = () => {
  var code = `var isArray = require("lodash/isArray");
isArray([1, 2, 3]);
// => true
`;
  return (
    <div>
      <Embed source={code} />
    </div>
  );
};

<Demo />

## MDN

[Array.isArray()](https://developer.mozilla.org/zh-TW/docs/Web/JavaScript/Reference/Global_Objects/Array/isArray)  

export const DeepDemo = () => {
  var code = `
Array.isArray([1, 2, 3]); // true
Array.isArray({ foo: 123 }); // false
Array.isArray("foobar"); // false
Array.isArray(undefined); // false
`;
return (
<div>
<Embed source={code} />
</div>
);
};

<DeepDemo />
