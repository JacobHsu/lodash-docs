# `_.isArrayBuffer(value)`

Checks if value is classified as an ArrayBuffer object.

## Arguments

`value (*)`: The value to check.

## Returns

`(boolean)`: Returns `true` if `value` is an array buffer, else `false`.

## Example

import Embed from "runkit-embed-react";

export const Demo = () => {
  var code = `var isArrayBuffer = require("lodash/isArrayBuffer");
isArrayBuffer(new ArrayBuffer(2));
// => true

isArrayBuffer(new Array(2));
// => false
`;
  return (
    <div>
      <Embed source={code} />
    </div>
  );
};

<Demo />

## Deep

export const DeepDemo = () => {
  var code = `
function isObjectLike(value) {
  return typeof value === 'object' && value !== null
}
function getTag(value) {
  if (value == null) {
    return value === undefined ? '[object Undefined]' : '[object Null]'
  }
  return toString.call(value)
}  
// const isArrayBuffer = nodeIsArrayBuffer
//   ? (value) => nodeIsArrayBuffer(value)
//   : (value) => isObjectLike(value) && getTag(value) == '[object ArrayBuffer]'
const isArrayBuffer = (value) => isObjectLike(value) && getTag(value) == '[object ArrayBuffer]'
isArrayBuffer(new ArrayBuffer(2));
// => true
`;
return (
<div>
<Embed source={code} />
</div>
);
};

<DeepDemo />
