# `_.slice()`

`_.slice(array, [start=0], [end=array.length])`  
Creates a slice of array from start up to, but not including, end.

**Note**: This method is used instead of [Array#slice](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/slice) to ensure dense arrays are returned.

## Arguments
`array (Array)`: The array to slice.  
`[start=0] (number)`: The start position.  
`[end=array.length] (number)`: The end position.

## Returns
`(Array)`: Returns the slice of array.

## Example

import Embed from 'runkit-embed-react';

export const Demo = () => {
var code =  `var slice = require("lodash/slice");
const animals = ['ant', 'bison', 'camel', 'duck', 'elephant'];
slice(animals, 1, 3)
// => ["bison", "camel"]
`   
  return (
    <div>
      <Embed source={code} />
    </div>
  );
};

<Demo/>

## Deep

export const DeepDemo = () => {
var code =  `function slice(array, start, end) {
let length = array == null ? 0 : array.length
start = start == null ? 0 : start
console.log(start);
end = end === undefined ? length : end
console.log(end);
const result = new Array(length)
result[0] = array[0 + start]
return result
}
const animals = ['ant', 'bison', 'camel', 'duck', 'elephant'];
slice(animals, 1, 3); 
// => ["bison", 4 empty]
`   
  return (
    <div>
      <Embed source={code} />
    </div>
  );
};

<DeepDemo/>
