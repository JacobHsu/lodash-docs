# `_.difference(array, [values])`

Creates an array of array values not included in the other given arrays using SameValueZero for equality comparisons. The order and references of result values are determined by the first array.

## Arguments
`array (Array)``: The array to inspect.
`[values] (...Array)`: The values to exclude.

## Returns
`(Array)`: Returns the new array of filtered values.

## Example

import Embed from 'runkit-embed-react';

export const Demo = () => {
var code =  `var difference = require("lodash/difference");
difference([2, 1], [2, 3]);
// => [1]
`   
  return (
    <div>
      <Embed source={code} />
    </div>
  );
};

<Demo/>

## Deep

export const Difference = () => {
var code =  `function difference(array, ...values) {
  return values;
}
difference([2, 1], [2, 3]);
// => [[2, 3]]
`   
  return (
    <div>
      <Embed source={code} />
    </div>
  );
};

<Difference/>

export const BaseFlatten = () => {
var code =  `function baseFlatten(array, depth) {
  const result = []
  if (array == null) {
    return result
  }
  for (const value of array) {
    if (depth > 0) {
      result.push(...value)
    }
  }
  return result
}
baseFlatten([[2, 3]], 1);
// => [2, 3]
`   
  return (
    <div>
      <Embed source={code} />
    </div>
  );
};

<BaseFlatten/>

