# `_.remove(array, [predicate])`

Removes all elements from array that predicate returns truthy for and returns an array of the removed elements. The predicate is invoked with three arguments: (value, index, array).


**Note**: Unlike `_.filter`, this method mutates array. Use `_.pull` to pull elements from an array by value.

## Arguments
`array (Array)`: The array to modify.  
`[predicate=_.identity] (Function)`: The function invoked per iteration.

## Returns
`(Array)`: Returns the new array of removed elements.

## Example

import Embed from 'runkit-embed-react';

export const Demo = () => {
var code =  `var remove = require("lodash/remove");
var array = [1, 2, 3, 4];
var evens = remove(array, function(n) {
  return n % 2 == 0;
});
console.log(array);
// => [1, 3]
console.log(evens);
// => [2, 4]
`   
  return (
    <div>
      <Embed source={code} />
    </div>
  );
};

<Demo/>

## Deep

export const DeepDemo = () => {
var code =  `function remove(array, predicate) {
  const result = []
  let index = 1
  const value = array[index] // => 2
  if( predicate(value, index, array) ) {
    result.push(value)
  }
  return result
}
var array = [1, 2, 3, 4];
remove(array, function(n) {
  return n % 2 == 0;
});
`   
  return (
    <div>
      <Embed source={code} />
    </div>
  );
};

<DeepDemo/>
